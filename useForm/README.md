# useForm Hook

Usage example:

```
    const initialForm = {
        name: 'String',
        age: Number,
        email: 'String'
    };

    const [ formValues, handleInputChange, reset ] = useForm( initialForm );

```
